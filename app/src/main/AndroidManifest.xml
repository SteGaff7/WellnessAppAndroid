<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loadmanager">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.WellnessEntriesListActivity"></activity>
        <activity android:name=".Activities.AddEntryRedirectActivity" />
        <activity android:name=".Activities.EntrySummaryActivity" />
        <activity android:name=".Activities.UserRegisterActivity" />
        <activity android:name=".Activities.UserLoginActivity" />
        <activity android:name=".Activities.HomeActivity" />
        <activity
            android:name=".Activities.EntryActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestActivites.LoginActivity" />
        <activity android:name=".TestActivites.APITestingActivity" />
        <activity android:name=".Activities.WellnessActivity" />
        <activity android:name=".TestActivites.MainActivity2" />
    </application>

</manifest>